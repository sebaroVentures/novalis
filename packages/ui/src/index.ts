// @novalis/ui
//
// Shared, app-agnostic UI primitives (Modal, DropdownMenu, buttons, etc.)
// reused across desktop and mobile. Populated as components are extracted from
// the legacy module.

export const UI_PACKAGE_NAME = "@novalis/ui";
